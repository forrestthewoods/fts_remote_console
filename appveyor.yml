shallow_clone: true

os:
  - Visual Studio 2015

environment:
  matrix:
  - TOOLSET: vs2015

platform:
  - Win32
  - x64

configuration:
  - debug
  - release

build:
  project: ./projects/$(TOOLSET)/fts_console.sln

artifacts:
  - path: ./projects/$(TOOLSET)/$(platform)_$(configuration)/**/*.exe
    name: test

notifications:

  # Email
  - provider: Email
    to:
      - lordcecil@gmail.com
    subject: 'fts_remote_console Build {{status}}'
    message: "{{message}}, {{commitId}}, ..."
    on_build_status_changed: true